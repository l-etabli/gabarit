{
  "name": "@letabli/gabarit",
  "module": "src/index.ts",
  "type": "module",
  "version": "0.0.0",
  "scripts": {
    "test": "bun test",
    "check": "biome check",
    "check:fix": "biome check --fix --no-errors-on-unmatched --files-ignore-unknown=true",
    "typecheck": "tsc --noEmit",
    "fullcheck": "bun run check && bun run typecheck && bun run test"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/bun": "latest",
    "lefthook": "^1.8.4",
    "typescript": "^5.7.2"
  }
}
